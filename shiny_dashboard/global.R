library(shiny)
library(shinydashboard)

# Placeholder data (replace later)
tracks <- data.frame(
  artist = c("Radiohead", "Beyoncé", "Kendrick Lamar"),
  track  = c("Paranoid Android", "Halo", "HUMBLE."),
  year   = c(1997, 2008, 2017)
)